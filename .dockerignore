# VCS
.git
.gitignore

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Virtual environments
.venv/
venv/

# Local caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/test-results/
frontend/playwright-report/
node_modules/

# jQuery / OS
*.log

# MLflow & run artifacts
mlruns/
mlflow.db
Phronesis_artifacts/
qa_artifacts/
storage/

# data/ (sample datasets) is deliberately NOT excluded: uploads are
# ephemeral on the Render free plan (no persistent disk), so the bundled
# samples are shipped in the image and re-seeded on every fresh start.

# Database files (mounted from volumes at runtime)
*.db
*.db-shm
*.db-wal
*.sqlite3

# Environment files
.env
.env.local
.env.*.local
.env.production