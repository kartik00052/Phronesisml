shared-workspace-lockfile=false
link-workspace-packages=false

# Peer dependency warnings for components with legacy peer ranges
# (e.g. echarts-for-react) are allowed; React is pinned to 18.x so these
# libraries are within supported runtime range.
strict-peer-dependencies=false