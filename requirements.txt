# Phronesis — pip-compatible dependency pins
# Canonical dependency source: pyproject.toml ([project.dependencies] and
# [project.optional-dependencies]). uv users should rely on uv.lock instead
# (uv sync). This file is a convenience for pip-only installs and matches the
# versions resolved in uv.lock at the time of the v0.3.0 release.
#
# Regenerate after dependency changes with:
#   uv pip install -e ".[dev,cli,excel,docs]" && uv pip freeze > requirements.txt
#
# Core
pydantic==2.13.4
langgraph==0.6.11
pandas==2.3.3
polars==1.43.2
scikit-learn==1.9.0
numpy==2.4.6
shap==0.52.0
pyarrow==19.0.1
joblib==1.5.3

# Excel (optional: pip install phronesisml[excel])
openpyxl==3.1.5

# CLI (optional: pip install phronesisml[cli])
typer==0.27.1
rich==15.0.0

# Docs (optional: pip install phronesisml[docs])
mkdocs==1.6.1
mkdocs-material==9.7.7
mkdocstrings[python]==0.30.1

# Dev / Test (optional: pip install phronesisml[dev])
pytest==8.4.2
pytest-asyncio==0.26.0
pytest-cov==5.0.0
pytest-xdist==3.8.0
coverage==7.15.3
httpx==0.28.1
ruff==0.16.1
mypy==1.20.2
pre-commit==4.6.1
build==1.5.0
twine==6.2.0
